@model IEnumerable<SacramentMeetingPlanner.Models.Speaker>

@{
    ViewData["Title"] = "Index";
}

<h1>Speakers</h1>

<p>
    <a asp-action="Create" asp-route-id="@ViewData["MeetingID"]" class="btn">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SpeakerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Topic)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SpeakerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Topic)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SpeakerID" class="btn">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SpeakerID" class="btn">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SpeakerID" class="btn">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
